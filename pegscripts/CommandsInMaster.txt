Commands in master:

sudo apt install -y python3-pip
pip3 install ujson
export PYSPARK_PYTHON=python3


Run spark job:
$SPARK_HOME/bin/spark-submit ./read_wat_spark.py
# remember to add --master and --packages if needed..
